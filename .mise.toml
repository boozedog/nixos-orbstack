[tasks."docker:build"]
description = "Build claude-code docker image and copy to macOS host"
run = "scripts/claude-code-docker.sh build"

[tasks."docker:run"]
description = "Load image and run claude-code in Docker"
raw = true
run = "scripts/claude-code-docker.sh run"
